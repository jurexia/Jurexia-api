fastapi>=0.109.0
uvicorn[standard]>=0.27.0
gunicorn>=21.2.0  # Process manager for uvicorn workers (graceful shutdown, --max-requests, --timeout)
pydantic>=2.5.0
# Tope fijado tras el incidente del 5-ago-2026: PyPI liberó la 1.19.0, que
# eliminó nombres del módulo de modelos, y el «>=» sin tope instaló la
# versión rota en el siguiente despliegue. El build pasó y el arranque
# murió. Subir el tope sólo tras probar el import completo de main.py.
qdrant-client>=1.7.0,<1.19
fastembed>=0.2.0
openai>=1.10.0
httpx>=0.26.0
python-dotenv>=1.0.0
python-docx>=1.1.0
olefile>=0.47
python-multipart>=0.0.6
supabase>=2.0.0
cohere>=5.0.0  # Reranking API (Phase 1 RAG optimization)
google-genai>=1.0.0  # Gemini 2.5 Pro for sentence drafting
PyMuPDF>=1.24.0  # Fast PDF text extraction (fitz) — fallback to OCR if not available
tenacity>=8.2.0  # Retry logic for API rate limits (429)
cryptography>=42.0.0  # OpenSSL / PKCS#8 / X.509 parsing for e.firma (.cer and .key)
pyhanko>=0.21.0  # Digital PDF PKCS#7 PAdES signing
app-store-server-library>=3.1.2  # Verifica los comprobantes firmados de StoreKit (ver apple_iap.py)

google-auth>=2.28.0  # OAuth de la cuenta de servicio para la Play Developer API (ver google_play.py)
# Codificador MP3 (rueda precompilada, sin dependencias del sistema). Hace falta
# para NIVELAR la voz: se pide PCM a ElevenLabs, se sube el volumen —Liza está
# grabada 22 dB por debajo de una voz normal— y se vuelve a comprimir.
lameenc>=1.8.4
